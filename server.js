const express = require('express');
const hbs = require('hbs');


const app = express();








const port = process.env.PORT || 3000;

app.listen('port', (req, res) => {
    console.log("cant wait to not get shit done on" + port);
})